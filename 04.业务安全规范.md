# 04. 业务安全规范

## 余额/算力

- [ ] 扣费操作使用数据库事务 + 行锁（SELECT ... FOR UPDATE）
- [ ] 扣费记录必须持久化（流水表），不依赖内存状态
- [ ] 余额/算力变更通过总账 + 流水表完成，不直接 UPDATE
- [ ] 不允许余额变成负数（CHECK 约束或事务中校验）
- [ ] 不允许负数扣费（扣费金额必须 > 0）
- [ ] 扣费前实时查库，不信任缓存中的余额
- [ ] 扣费失败必须回滚完整事务
- [ ] 充值/退款等操作记录操作人和操作来源
- [ ] 算力/余额变更有完整的出入账流水

## 签到

- [ ] 使用 Redis Set 或数据库唯一索引防止重复签到
- [ ] 签到记录不可修改、不可删除（仅可补签）
- [ ] 补签使用单独的 API，记录补签原因

## 奖励领取

- [ ] 每个奖励活动设置唯一领奖记录（联合唯一索引）
- [ ] 奖励发放使用事务：扣减库存 → 创建记录 → 发放奖励
- [ ] 奖励库存使用乐观锁防止超发
- [ ] 相同奖励不可重复领取
- [ ] 奖励发放失败时自动回滚

## 兑换

- [ ] 兑换码使用唯一索引
- [ ] 兑换码支持批量生成，但需记录每批次的生成人和生成时间
- [ ] 同一兑换码不可重复使用
- [ ] 兑换时锁定库存（SELECT ... FOR UPDATE 或 Redis 分布式锁）
- [ ] 兑换失败时恢复库存

## 订单

- [ ] 不允许重复创建订单（订单号使用 UUID 或唯一流水号）
- [ ] 订单金额在服务端计算，不接受客户端传入
- [ ] 订单状态变更使用状态机，不允许跳状态
- [ ] 取消订单需校验当前状态是否允许取消
- [ ] 不允许修改订单金额（如需修改，走退款后重新下单流程）
- [ ] 不允许修改商品价格（价格在商品表中锁定）

## 支付

- [ ] 支付回调使用独立 Webhook 端点，不对外暴露
- [ ] 支付回调校验签名（HMAC 或 RSA）
- [ ] 不允许重复支付成功回调（以支付渠道的 transaction_id 为唯一键）
- [ ] 支付回调处理使用幂等性设计
- [ ] 支付回调 IP 加入白名单
- [ ] 退款使用原路返回
- [ ] 不允许重复退款（每个退款请求有唯一退款单号）
- [ ] 退款金额不得超过实际支付金额
- [ ] 退款需要审核流程（大额退款人工审核）

## VIP/用户等级

- [ ] VIP 等级变更记录操作日志
- [ ] VIP 过期时间在服务端计算
- [ ] 不允许客户端传入 VIP 等级或过期时间
- [ ] 用户角色修改需要管理员权限
- [ ] 用户角色修改记录操作人、操作时间、原因

## 通用检查

- [ ] 不允许越权取消别人任务
- [ ] 不允许越权删除别人数据
- [ ] 不允许越权修改别人数据
- [ ] 不允许通过修改请求参数伪造他人身份
- [ ] 不允许重复创建/生成 AI 任务（每个任务有唯一任务号）
- [ ] 不允许重复上传（使用文件哈希 + 唯一索引）

## 生成任务与算力（聚梦画布 · 性能方案）

> [聚梦画布-生成任务性能改造方案](./聚梦画布-生成任务性能改造方案.md) §3.1、§4.2、§10.4、§11.7。在 **Submit Worker 事务内预扣** 后，下列规则仍适用。

- [ ] 预扣幂等键固定 `canvas-job-{jobId}`；MQ 重复消费 **不得** 重复扣 lot
- [ ] 顺序：**写 job 行 + 预扣成功 → 再 POST 上游**；预扣失败则 `failed`，**禁止** 调上游
- [ ] 收尾（Poll 内下载/OSS/`commit_for_job`）按 jobId 幂等；已成功跳过
- [ ] 上游超时：**先补查**再 `release_for_job`；禁止误退（性能方案 §4.4、§6.4）
- [ ] `commit_pending` / `release_pending` 须 reconcile；改造后中间态增多，监控见 11.日志审计
- [ ] API 余额预检读 Redis 时：**不得以缓存余额作为唯一扣费依据**；权威仍以 lot 事务为准
- [ ] 双轨灰度：同一 jobId **不得** 既被旧 Worker claim 又被 MQ 路径处理（性能方案 §11.5）

### 前端「再次生成」（安全架构意见 · 不改代码）

> 完整意见见性能方案 **§10.4**。本节为 **给研发的检查清单**；安全架构师 **只出意见，不改代码**。

**禁止（评审时否决）**

- [ ] 需求/实现中出现 **全局或长时间** 锁死生成（成功后永久不可再点、整页长时间冷却等）
- [ ] 用前端锁 **替代** 后端 lot 预扣与幂等
- [ ] 防误触导致 **失败重试、改参数再生成、分镜重试** 不可用

**必须保留**

- [ ] 后端资损防护：lot 预扣、`dedupe_key`、同一 job 不重复扣（见上文生成任务条目）
- [ ] 产品上的 **合法再次生成** 能力（§10.4 清单）
- [ ] 请求飞行中的 `generating` / disabled（正常行为，勿与「永久锁」混淆）

**可选（研发自行决定是否做；安全不强制）**

- [ ] 极窄误触防抖：同节点 + 同参数 + 约 2 秒内连点；参数变或超时后 **必须** 可再点
- [ ] 若做防抖，验收：**误触少发 job + 合法第二次生成仍可用**

## 项目协同（聚梦画布）

> 完整规范：[17.项目协同安全规范](./17.项目协同安全规范.md)

### 成员与邀请

- [ ] 仅 owner 可邀请/移除；不可邀请自己或 owner 账号
- [ ] 成员数上限 enforced（DB + 接受邀请时二次校验）
- [ ] pending 成员 **不可** 访问画布与 OSS 资源
- [ ] 成员变更加锁，避免并发突破上限

### 协作算力（扣 owner，记 actor）

- [ ] 协作者生成：`billing_user = project.owner`，`actor_user_id = 当前用户`
- [ ] 预扣/结算/释放只动 **owner** 的 lot，不动协作者 lot
- [ ] 日限额、总限额仅统计协作者在该项目的消耗
- [ ] 超审批阈值 → `AWAITING_APPROVAL`，未批准前 **不预扣**
- [ ] owner 批准时再次校验 cap 与 owner 余额，事务内预扣
- [ ] 协作者 **不能** 修改协作限额或批准自己的任务

### 协同不意味着放松通用规则

- [ ] 仍须 `quote_generation_cost` 计价、`quoteToken` 或 pricingVersion 校验
- [ ] 仍须 `dedupe_key` / 幂等防重复提交
- [ ] 仍须 `release_for_job` 失败退款，且可追溯 `actor_user_id`
