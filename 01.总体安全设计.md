# 01. 总体安全设计

## 安全架构原则

### 纵深防御（Defense in Depth）

不依赖单一安全机制，在网络层、主机层、应用层、数据层分别部署防护。

### 最小权限（Least Privilege）

每个组件、每个用户、每个服务只授予完成工作所需的最小权限。

### 默认安全（Secure by Default）

所有配置默认启用安全选项，开放功能需要显式授权。

### 永不信任（Zero Trust）

内部请求也需要认证和鉴权，不因网络隔离而信任。

## 统一安全框架组件

建议将安全逻辑抽象为公共中间件/装饰器，而非散落在业务代码中。

### SecurityMiddleware

```python
# 核心安全中间件，统一注入所有安全处理
class SecurityMiddleware:
    def __init__(self, app):
        self.app = app

    async def __call__(self, request, call_next):
        # 1. RequestId 注入
        # 2. SQL 注入检测
        # 3. XSS 过滤
        # 4. HTTP 安全头注入
        # 5. 敏感数据脱敏
        # 6. 耗时记录
        response = await call_next(request)
        return response
```

### AuthMiddleware

认证中间件，负责 Token 校验、会话管理。

### PermissionMiddleware

权限中间件，负责 RBAC 校验、数据权限过滤。

### RateLimitMiddleware

限流中间件，支持 IP、用户、Token、接口等多维度限流。

### IdempotentMiddleware

幂等中间件，防止重复提交。

### AuditMiddleware

审计中间件，记录所有敏感操作。

### OperationLogMiddleware

操作日志中间件，记录修改前后数据。

### RequestIdMiddleware

请求追踪中间件，注入全局 RequestId / TraceId。

### SensitiveDataMiddleware

敏感数据脱敏中间件，自动过滤响应中的敏感字段。

### SQLInjectionFilter

SQL 注入过滤器，检测并拦截恶意请求。

### XSSFilter

XSS 过滤器，对输入进行安全清理。

### CSRFMiddleware

CSRF 防护中间件。

### CSPMiddleware

Content-Security-Policy 中间件。

### UploadSecurityMiddleware

上传安全中间件，校验文件类型、大小、内容。

### DownloadSecurityMiddleware

下载安全中间件，校验权限、路径、签名。

### JWTMiddleware

JWT 中间件，校验签名、过期、刷新。

### ExceptionMiddleware

统一异常处理中间件，防止信息泄露。

## 安全编码规范

1. 所有用户输入视为不可信
2. 输出编码防止 XSS
3. 使用参数化查询防止 SQL 注入
4. 密码使用 bcrypt/argon2 哈希
5. 敏感信息不在日志、URL、响应中泄露
6. 所有 API 必须认证（白名单例外）
7. 使用 HTTPS 传输
8. 遵循 OWASP Top 10 防护

## 安全开发生命周期

```
需求阶段 → 安全需求评审
设计阶段 → 威胁建模（STRIDE）
开发阶段 → 安全编码 + 静态扫描
测试阶段 → 安全测试 + 渗透测试
上线阶段 → 安全验收 + 配置审计
运维阶段 → 监控告警 + 应急响应
```

## 项目协同（聚梦画布）

多用户共享同一画布时，在认证层之上增加 **项目级授权** 与 **协作算力规则**。  
不改变 HMAC 登录；详见 [17.项目协同安全规范](./17.项目协同安全规范.md)。

## 生成任务架构与安全边界（聚梦画布）

> 架构权威：[聚梦画布-生成任务性能改造方案](../ai_read/聚梦画布-生成任务性能改造方案.md)（下称「性能方案」）。  
> 部署基线：[14.Linux安全](./14.Linux安全.md)、[聚梦画布生产部署架构](../ai_read/聚梦画布生产部署架构.md)。

安全规范须在该架构上 **叠加约束**，不得要求回退到「API 同步写大表 + 取消 MQ」等与方案冲突的做法。

| 层级 | 性能方案职责 | 安全须保证 |
|------|--------------|------------|
| **API** | 鉴权/项目权限/报价校验 → Redis(pending) + MQ submit（**不写** `generation_jobs` 大表） | `Idempotency-Key`、`quoteToken` 在 API 固化进 MQ 消息；多 uvicorn 进程下 Redis `SETNX` + publisher confirm |
| **Submit Worker** | MQ 消费 → 事务写库 + **预扣算力** → POST 上游 | 预扣幂等 `canvas-job-{jobId}`；余额不足 **不** 调上游；协作者仍扣 **owner** lot |
| **Poll Worker** | Redis ZSET 闹钟 + 上游 GET + 异步收尾 OSS | 全局限流防打爆上游；`failed` 才退款；超时先补查再退 |
| **MySQL** | 唯一真相 | `dedupe_key` 唯一索引兜底；Poll 只扫进行中任务 |
| **Redis** | 加速层（状态/闹钟/限流） | 前缀 `canvas:*` 与 SMS key 隔离；**非**权威余额/扣费结果 |
| **MQ** | submit 持久队列 | VPC 内网 `172.18.142.123:5672`；持久化 + confirm；见 14.Linux |

**分期**：video lane 先拆 Submit/Poll（双轨灰度）；text_llm / audio 可暂保留 monolithic Worker——安全 checklist 按 **已上线阶段** 勾选，未启用 MQ 的项标为「改造后」。
