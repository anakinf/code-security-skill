# 09. 文件上传下载

## 上传安全

### 目录隔离

- [ ] 不同用户的上传文件存放在不同目录（`uploads/{user_id}/`）
- [ ] 不同业务的上传文件存放在不同目录（`uploads/{business}/{user_id}/`）
- [ ] 上传目录不在 Web 根目录下
- [ ] 上传目录禁止脚本执行

### 文件名安全

- [ ] 使用随机文件名（UUID），不使用用户原始文件名
- [ ] 文件后缀全小写化
- [ ] 文件后缀仅允许白名单内的类型
- [ ] 上传前清理文件名中的路径穿越字符（`../`、`..\\`）

### 文件类型校验

- [ ] 不信任 `Content-Type` 头（客户端可伪造）
- [ ] 检查文件魔数（Magic Number），而非仅后缀名
  ```python
  import magic

  ALLOWED_MIME_TYPES = {
      'image/jpeg', 'image/png', 'image/gif', 'image/webp',
      'application/pdf',
  }

  mime = magic.from_buffer(file_bytes, mime=True)
  if mime not in ALLOWED_MIME_TYPES:
      raise ValueError("File type not allowed")
  ```
- [ ] 图片文件额外校验：尝试 `PIL.Image.open()` 打开
- [ ] 禁止上传 HTML、SVG（含 XSS 风险）、.exe、.bat、.sh 等

### 文件内容校验

- [ ] 图片文件重编码（防止图片马）
- [ ] SVG 文件净化（移除 script 标签）
- [ ] PDF 文件扫描
- [ ] 压缩包解压时限制路径穿越

### 文件大小限制

- [ ] 图片：≤ 10MB
- [ ] PDF：≤ 50MB
- [ ] 视频：≤ 500MB（如支持上传）
- [ ] Nginx 层也限制上传大小（client_max_body_size）

### 覆盖防护

- [ ] 不允许覆盖已有文件（先检查文件是否存在）
- [ ] 同一文件名自动加后缀（`xxx_1.jpg`）

## OSS 对象存储安全

### 通用

- [ ] 上传目录隔离（不同用户/业务使用不同前缀）
- [ ] 禁止覆盖已有文件
- [ ] 使用随机文件名
- [ ] 限制文件类型（服务端校验）
- [ ] 限制 Content-Type（防止恶意文件上传）
- [ ] 限制文件大小
- [ ] 不允许公开 Bucket
- [ ] 不允许目录浏览（关闭 ListObject 权限）

### 访问控制

- [ ] 所有文件通过签名 URL 访问（presigned URL）
- [ ] 签名 URL 设置过期时间

## 项目目录隔离（协同）

聚梦画布 OSS 按 **`projects/{projectId}/`** 隔离；协作者与 owner 写入 **同一项目前缀**，但须：

- [ ] 上传/注册资产前 `require_project_access`
- [ ] `sign-url` / `batch` 解析时校验 key 或 asset 属于请求中的 `projectId`
- [ ] 禁止通过篡改 `projectId` 或 storage key 访问其他项目对象
- [ ] Poll Worker 收尾上传 OSS 仍须写入 **同一** `projects/{projectId}/` 前缀（见 [性能方案](../ai_read/聚梦画布-生成任务性能改造方案.md) §3.3）
- [ ] 协作者 **不能** 将其他项目 assetId 引用到当前项目节点（服务端校验 project 归属）（建议 ≤ 30min）
- [ ] 私有 Bucket，不设置公开读
- [ ] 使用 IAM 角色而非 AccessKey
- [ ] 定期轮换 AccessKey

### 权限策略

- [ ] 最小权限原则：应用只拥有上传/下载权限
- [ ] 不使用 Admin 权限的 AccessKey
- [ ] 资源级授权：`arn:aws:s3:::bucket/prefix/*`

## 下载安全

- [ ] 所有下载请求经过后端鉴权
- [ ] 下载 URL 使用临时签名
- [ ] 下载时校验用户是否有下载该文件的权限
- [ ] 响应头设置 `Content-Disposition: attachment`
- [ ] 响应头设置 `X-Content-Type-Options: nosniff`
- [ ] 外部存储下载时，文件先经过代理，不直接暴露 OSS URL
