# 02. 认证授权规范

## JWT 规范

### 基础要求

- [ ] Secret 使用强随机字符串（≥256位，推荐 openssl rand -hex 64）
- [ ] 签名算法使用 RS256 或 HS256
- [ ] 不使用 `none` 算法
- [ ] 验证 Token 时校验 `alg` 头，防止算法混淆攻击
- [ ] Token 设置合理的过期时间（AccessToken ≤ 15min，RefreshToken ≤ 7天）
- [ ] 不使用过长的过期时间
- [ ] 密钥定期轮换（支持多密钥同时生效，通过 kid 区分）

### Token 管理

- [ ] 支持 Token 立即失效（黑名单机制 Redis Set）
- [ ] 支持 RefreshToken 失效
- [ ] 支持踢人下线
- [ ] 支持单设备登录控制
- [ ] 支持多设备登录数量限制
- [ ] 防止 Token 重放攻击（使用 jti + 已使用集合）
- [ ] Token 不在 URL 参数中传递
- [ ] Token 存储在 HttpOnly Cookie 或 Authorization Header

### Session 安全

- [ ] Session ID 足够随机
- [ ] Session 设置过期时间
- [ ] 登出时清除 Session
- [ ] Session 绑定 IP 或 User-Agent（可选）

## RBAC 权限规范

### 模型设计

```
用户 → 角色 → 权限
用户 → 角色（部门/项目维度的数据权限）
```

- [ ] 权限控制到接口级别
- [ ] 支持数据权限（仅看自己、本部门、全部）
- [ ] 接口权限与数据权限分离
- [ ] 默认拒绝（白名单模式）
- [ ] 权限变更即时生效（通过 Redis 缓存权限）
- [ ] 管理端与用户端权限体系分离
- [ ] 越权检查：A 用户不能操作 B 用户的数据

### 越权防护

- [ ] 所有涉及用户ID的操作，从 Token 中取当前用户 ID，而非客户端传入
- [ ] 修改/删除操作必须校验资源所有权
- [ ] 管理接口与普通接口分离（不同路由前缀）
- [ ] 管理接口额外校验管理员角色
- [ ] 伪造管理员请求防护（签名 + IP白名单）

## OAuth2.0 / 第三方登录

- [ ] 使用 state 参数防止 CSRF
- [ ] 验证 redirect_uri 白名单
- [ ] 绑定后立即失效临时 code
- [ ] 第三方账号绑定需验证当前登录用户

## 项目级授权（协同场景）

> 通用 RBAC 之上，画布类 SaaS 常见 **Owner / Editor** 项目角色。  
> 聚梦画布完整清单见 [17.项目协同安全规范](./17.项目协同安全规范.md)。

### 聚梦画布现状（保留现有登录，只防越权）

- **认证不变**：HMAC 会话 + `get_current_user`；本节 **不要求** 切换为 JWT
- **授权入口**：`require_project_access`（Owner/Active Editor）、`require_project_owner`（仅 Owner）
- **非成员**：返回 `PROJECT_NOT_FOUND`，不区分「不存在」与「无权限」

### 必须遵守

- [ ] 带 `projectId` 的 API **必须** 校验 membership，不能仅校验「已登录」
- [ ] 子资源（workflow、job、asset）须校验归属同一 `projectId`
- [ ] 当前用户 ID 只从会话取，**禁止**信任客户端传入的 `userId` / `ownerId`
- [ ] Owner 专属操作（邀请、移除、协作限额、审批生成）**必须** `require_project_owner`
- [ ] 邀请接受/拒绝 **必须** 是被邀请人本人会话
- [ ] 前端隐藏按钮仅为 UX；**后端** 为最终授权

### 协同开发友好原则

- 允许 Editor 编辑画布、保存工作流、上传资产、提交生成——**不要** 在规范或实现中默认禁止协作者写操作
- 禁止的是 **跨项目读写** 与 **Editor 冒充 Owner**，不是协同本身

### 生成任务查询（性能方案）

> [聚梦画布-生成任务性能改造方案](../ai_read/聚梦画布-生成任务性能改造方案.md) §6。`GET /generations/{jobId}` **仍须** 校验当前用户对 `job.project_id` 有 `require_project_access`；Redis 缓存命中也不能跳过鉴权。
