# 10. AI 接口安全

## Prompt 安全

- [ ] 防止 Prompt Injection（用户输入注入到系统 Prompt 中）
  - 系统 Prompt 与用户输入严格隔离
  - 用户输入使用分隔符包裹或结构化传入
  - 不使用字符串拼接构建 Prompt
- [ ] 校验 AI 输出，防止注入到下游系统
- [ ] 对 AI 输出进行内容安全审核（敏感词过滤）
- [ ] 不将 API Key / Secret 传入 Prompt

## 请求限制

### Token 限制

- [ ] 限制每次请求的最大 Token 数（max_tokens）
- [ ] 限制请求的最大上下文长度
- [ ] 响应超长时自动截断或分页
- [ ] 监控平均 Token 消耗，预估成本

### 媒体文件限制

- [ ] 限制图片最大尺寸（宽高）
- [ ] 限制图片最大文件大小
- [ ] 限制单次请求图片最大数量
- [ ] 限制视频最大大小
- [ ] 限制 PDF 最大大小
- [ ] 限制单次上传文件数量
- [ ] 对图片进行压缩后再发送给 AI 服务

### 并发限制

- [ ] 限制单用户同时生成的请求数
- [ ] 限制全局并发生成请求数
- [ ] 限制队列最大长度（排队策略）
- [ ] 超出限制时返回 429 错误，而非静默丢弃
- [ ] 提供排队状态查询接口

### 额度限制

- [ ] 限制每日使用额度（区分免费/付费用户）
- [ ] 限制每分钟请求数（Rate Limit）
- [ ] 限制每 IP 请求数
- [ ] 限制每账号请求数
- [ ] 额度不足时提前告知用户
- [ ] 支持预付费/后付费额度扣减

## 幂等与防重

- [ ] 每个 AI 生成任务有唯一 ID
- [ ] 防止重复生成：以任务 ID 为幂等键
- [ ] 防止无限重试：设置最大重试次数（建议 3 次）
- [ ] 客户端重试时使用相同任务 ID
- [ ] 重复请求返回相同任务结果

## 自动退款与补偿

- [ ] AI 调用失败时自动退款（退回已扣算力/余额）
- [ ] AI 调用超时时自动退款
- [ ] 用户取消 AI 任务时自动退款
- [ ] 退款记录与充值流水关联

## 流水与审计

- [ ] AI 接口记录完整调用流水
  - 请求时间、用户 ID、任务 ID
  - 输入摘要（非完整内容）、输出摘要
  - Token 消耗（输入/输出）
  - 耗时、状态（成功/失败）
  - 错误码、错误信息
  - 模型版本、参数
- [ ] 流水可供用户查询（我的用量）
- [ ] 流水可供管理员审计
- [ ] 支持导出使用报表

## 超时控制

- [ ] 设置请求超时时间（建议 ≤ 120s）
- [ ] 超时后自动取消请求
- [ ] 超时后释放资源
- [ ] 超时后通知客户端（回调或轮询）

## 回退策略

- [ ] 主模型失败时自动降级到备用模型
- [ ] 备用模型预热（保持连接池）
- [ ] 降级时记录日志
- [ ] 降级后额度扣减按实际使用模型计算

## 协作者生成（共享项目）

> 与 [17.项目协同安全规范](./17.项目协同安全规范.md) 一致；**允许** 协作者提交生成，但必须：

- [ ] 提交前 `require_project_access`；计费主体解析为 **项目 owner**
- [ ] `GenerationJob` 区分 actor 与 billing 用户（审计：谁点的、扣谁的）
- [ ] 超 `collaborator_approval_threshold` 时不得直接入队，须 owner 批准
- [ ] 批准/拒绝 API 仅 owner；拒绝时不产生预扣
- [ ] 协作者并发提交受 owner 侧锁与 cap 约束
- [ ] 任务查询须校验 job 属于当前用户 **有权限的项目**

## 生成链路（聚梦画布 · 性能方案）

> [聚梦画布-生成任务性能改造方案](../ai_read/聚梦画布-生成任务性能改造方案.md) §4.1、§4.4、§11.7。

- [ ] **全局限流**：每个上游（DashScope/Vidu/Ark）全站 QPS 上限（Redis 令牌桶）；Poll GET 前拿令牌
- [ ] 提交上游幂等 key：`canvas-job-{jobId}`（与算力预扣键一致）
- [ ] Poll 收尾下载池 **有上限**（如 20 并发），防 Worker ECS 内存/带宽打满
- [ ] 可选 Webhook：校验来源 + 仅加速 `next_poll_at`，轮询仍作兜底
- [ ] 429/上游失败：记录指标；**不** 因单次失败无限重试导致重复扣费（幂等 + 状态机）
- [ ] video/image lane 改造后：text_llm/audio 仍可 monolithic；安全规则按 lane **已启用阶段** 适用
