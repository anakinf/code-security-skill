# 14. Linux 安全

## 文档定位

本文 **不要求改动现有部署**，只描述聚梦画布 **当前生产模式**，并在该模式上给出 **可选** 加固建议。

- **基线**：已与 `canvas/scripts/deploy/`、`ai_read/聚梦画布生产部署架构.md`、`ai_read/聚梦画布-生成任务性能改造方案.md` 对齐；运维按现有脚本（`deploy_remote.py` / rsync + systemd）即可。
- **建议**：标为「可选」的条目，有余力、且 **验证不影响业务** 后再做；**禁止** 为凑 checklist 而关闭密码 SSH、改 systemd 用户、或在 MQ 上乱开主机防火墙。

> 换宽带或出差时，若 SSH 连不上，先查 **安全组源 IP 白名单**，不要先改 sshd 或防火墙。

---

## 生产现状基线（当前采用，勿随意改）

### ECS 与 SSH

| 机器 | 公网 IP | 内网 IP | SSH 用户 | SSH 端口 | 应用进程用户 |
|------|---------|---------|----------|----------|--------------|
| **API ECS** | `121.43.168.38` | — | `ecs-user` | **520**（备选 22） | `ecs-user`（web / api） |
| **Worker ECS** | `121.41.205.4` | `172.18.142.114` | `root` | **22** | `root`（4 lane worker） |
| **MQ ECS** | `47.111.28.103` | `172.18.142.123` | `root` | **22** | RabbitMQ（root 安装/运维） |

- SSH：**密码登录 + 安全组仅放行运维固定公网 IP**（不强制关 `PasswordAuthentication`，便于 deploy 脚本）。
- 代码目录：两台 ECS 均为 `/opt/jumeng-canvas`；生产 env：`/etc/jumeng-canvas/api.env`、`web.env`、`worker.env`（勿提交 git）。

### API ECS 服务与端口

| 组件 | 监听 | 对外 | systemd 用户 |
|------|------|------|--------------|
| 宝塔 Nginx | 80 / 443 | `https://v.jumengai.com/canvas/` | — |
| Next.js | `127.0.0.1:3001` | 经 Nginx 反代 | `ecs-user` |
| FastAPI | `127.0.0.1:8001` | **不** 直出公网，经 Next BFF | `ecs-user` |

### Worker ECS（4 lane）

| systemd 单元 | lane | 默认并发 | 用户 |
|--------------|------|----------|------|
| `jumeng-canvas-worker-image` | `image` | 22 | `root` |
| `jumeng-canvas-worker-video` | `video` | 8 | `root` |
| `jumeng-canvas-worker-text` | `text_llm` | 12 | `root` |
| `jumeng-canvas-worker-audio` | `audio` | 8 | `root` |

工作目录：`/opt/jumeng-canvas/packages/api`；`WORKER_ENABLED=1`。

### MQ ECS（RabbitMQ）

| 端口 | 用途 | 访问方式（现状） |
|------|------|------------------|
| **5672** | AMQP | API / Worker 经 **内网** `172.18.142.123` 连接 |
| **15672** | 管理台 | 运维公网 IP + 安全组 |
| **22** | SSH | 运维公网 IP + 安全组 |

应用连接串（写入 `api.env` / `worker.env`，勿提交 git）：

```
amqp://jumeng_canvas_app:***@172.18.142.123:5672/jumeng_canvas
```

> **与生成任务改造的关系**（[性能方案](../ai_read/聚梦画布-生成任务性能改造方案.md) §11.3）：阶段 2 起 API 发 **submit** 队列、Worker 消费；须 **持久化 + publisher confirm**。改造前生产 **无 MQ**，本节网络基线仍有效，应用侧 MQ 集成按方案分期上线，**勿因安全文档提前改断 5672**。

### 托管服务（不经 ECS 进程用户）

| 服务 | 地址 | 暴露 |
|------|------|------|
| MySQL RDS | 内网 `rm-bp1z3unn7emn70s2z.mysql.rds.aliyuncs.com:3306` | 仅 VPC，**禁止** 公网 |
| Redis | 内网 TLS `6379`，`REDIS_DB=1` | 仅 VPC |
| OSS | 内网 Endpoint + 签名 URL | Bucket 私有 |

---

## 已有安全边界（维持即可）

1. **安全组**：SSH（520/22）、443/80、MQ 5672/15672 均按 IP / 网段白名单；不开放 `0.0.0.0/0` 到 SSH 或 MQ AMQP。
2. **应用不直出**：FastAPI 只监听 localhost；媒体走 OSS 签名 URL。
3. **密钥与 env**：`env.sh`、`llm-keys.env` 已 gitignore；生产 env 在 `/etc/jumeng-canvas/`。
4. **主机防火墙**：API / Worker / MQ **当前均未启用** `firewalld`/`ufw`，业务正常——**保持此状态即可**，勿为「加固」贸然启用。

---

## SSH

### 现状（与 deploy 一致）

- API：`ecs-user` @ **520**；Worker / MQ：`root` @ **22**。
- 密码登录；安全组限制源 IP 后，公网 brute force **难以触达** SSH。

### 可选建议（做了要验证 deploy 仍可用）

| 建议 | 说明 |
|------|------|
| 强密码 | SSH 账号定期换强密码（≥16 位） |
| 密钥登录 | 可与密码并存；**勿**在未改 deploy 脚本前单独 `PasswordAuthentication no` |
| 空闲断开 | `ClientAliveInterval 300` / `ClientAliveCountMax 2` |
| 登录日志 | 关注 `/var/log/secure` 或 `auth.log` 异常 IP |
| Fail2Ban | 安全组已限 IP 时 **优先级低**；API ECS 可作纵深，**非必须** |

### 勿做（会破坏现有运维）

- ~~强制仅密钥、关密码~~（`deploy_remote.py` / rsync 依赖当前方式）
- ~~Worker/MQ 改非 root 跑应用或禁止 root SSH~~（与 systemd / 运维习惯不一致）
- ~~API ECS 上 `AllowUsers ecs-user` 写进 sshd 却忘了 Worker/MQ 仍用 root~~（各机配置独立，勿套模板）

---

## 防火墙与安全组

**原则：业务连通优先。** 改安全组或主机防火墙前，先确认 API → RDS/Redis/OSS、API/Worker → MQ `172.18.142.123:5672` 不受影响。

### 现状

- **主防火墙 = 阿里云安全组**（入站白名单）。
- **不启用** 主机 `firewalld` / `ufw`（三台 ECS 现状）。

### 安全组（维持 / 变更时核对）

| 目标 | 端口 | 建议范围 |
|------|------|----------|
| API ECS | 443, 80 | 公网（经 WAF/Nginx） |
| API ECS | 520（或 22） | **仅运维固定公网 IP** |
| Worker ECS | 22 | **仅运维 IP** |
| MQ ECS | 5672 | **仅 VPC 内网**（API `121.43.168.38`、Worker `121.41.205.4` 所在网段） |
| MQ ECS | 15672, 22 | **仅运维 IP** |
| RDS / Redis | 3306 / 6379 | **仅 VPC**，禁止公网 |

### MQ 特别注意（勿影响 submit 队列）

```
运维 ──(公网 IP 白名单)──► MQ:22 / MQ:15672
API ECS ──(VPC 内网)────► MQ:172.18.142.123:5672
Worker ECS ─(VPC 内网)──► MQ:172.18.142.123:5672
```

- **禁止** 在 MQ 上执行「`ufw default deny incoming` + 只放行 22」——会 **掐断** API/Worker 到 5672 的内网连接。
- 若将来必须在 MQ 启用主机防火墙，须 **显式放行** VPC 来源 → `5672/tcp`，改完用内网 Host `172.18.142.123` 做 publish/consume 复测。
- 收紧安全组前，先在 API/Worker 上跑连通性测试（如桌面 `test_rabbitmq_canvas.py`），**通过后再改**。

### 可选建议（非 MQ 机器、且确认不影响出站 MQ）

- 关闭 FTP、Telnet 等无用服务。
- 若 API/Worker 要启 ufw，须 **保留默认 allow outgoing**，且 **不得** 阻断到 `172.18.142.123:5672` 的出站。

---

## 用户与权限

### 现状（不改为宜）

| 机器 | SSH | 应用 / 服务 |
|------|-----|-------------|
| API ECS | `ecs-user` | web、api → `ecs-user` |
| Worker ECS | `root` | 4 worker → `root` |
| MQ ECS | `root` | RabbitMQ 运维 → `root` |

安全主要靠 **安全组 + env 文件权限 + 最小端口暴露**，不强制单独 deploy 用户或 `nologin` 应用账号。

### 可选建议

- `/etc/jumeng-canvas/*.env` 权限 **600**（root 或对应部署用户可读即可）。
- 上传目录避免 `777`；定期扫 SUID/SGID（`find / -perm /6000 -type f 2>/dev/null`）。

---

## 系统加固与审计（均可选）

以下 **非上线硬性要求**；按需、低峰、可回滚地实施：

| 类别 | 可选项 |
|------|--------|
| 内核 | `sysctl` 关闭源路由、icmp 广播等（见通用 hardening 模板） |
| 补丁 | `unattended-upgrades` 或定期 `yum/apt` 安全更新；内核升级后择期重启 |
| 完整性 | AIDE / Tripwire、rkhunter（资源与误报成本较高） |
| 日志 | logwatch、auth.log 异常登录告警 |
| Fail2Ban | 见上文 SSH 节 |

---

## 与上线 Checklist 的关系

`15.上线Checklist.md` 中 Linux / MQ / SSH 条目以 **本文「生产现状基线」** 为准。Checklist 里与现状冲突的「必须改用户 / 必须关密码 / 必须启主机防火墙」**以本文为准，不要求执行**。

生成任务、MQ、Redis job key 等安全验收见 Checklist **「生成任务架构」** 节，并与 [聚梦画布-生成任务性能改造方案](../ai_read/聚梦画布-生成任务性能改造方案.md) 分期对照。
