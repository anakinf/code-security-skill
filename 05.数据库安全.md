# 05. 数据库安全

## 权限管理

- [ ] 数据库使用最小权限账号，不用 root 连接应用
- [ ] 每个应用使用独立数据库账号
- [ ] 只授予应用所需的最小权限（SELECT, INSERT, UPDATE, DELETE）
- [ ] 禁止 root 远程连接
- [ ] 定期清理无用账号
- [ ] 关闭危险权限（FILE, SUPER, PROCESS, SHUTDOWN）
- [ ] 存储过程、触发器、事件使用最小权限

## 连接安全

- [ ] 启用 SSL/TLS 加密连接
- [ ] 限制应用服务器 IP 访问（iptables 或安全组）
- [ ] 开启连接池（如 SQLAlchemy 连接池）
- [ ] 限制最大连接数（max_connections）
- [ ] 设置连接超时（wait_timeout, interactive_timeout）
- [ ] 连接池中验证连接有效性（pool_pre_ping）

## SQL 注入防护

- [ ] 全部使用参数化查询（ORM 或 prepared statement）
- [ ] 禁止拼接 SQL 字符串
- [ ] 动态排序字段使用白名单校验
- [ ] 动态表名使用白名单校验
- [ ] 存储过程也使用参数化

## 数据保护

- [ ] 密码使用 bcrypt/argon2 哈希存储
- [ ] 手机号、身份证等敏感数据使用 AES 加密存储
- [ ] 敏感字段查询时脱敏（如手机号中间四位隐藏）
- [ ] 数据库备份文件加密存储
- [ ] 备份文件在不同地域/机房存储

## 审计与监控

- [ ] 开启慢 SQL 日志（long_query_time ≤ 1s）
- [ ] 开启通用查询日志（仅审计期间）
- [ ] 开启错误日志
- [ ] 定期分析慢查询并优化
- [ ] 数据库变更记录变更日志

## 备份与恢复

- [ ] 每天自动备份（mysqldump 或 XtraBackup）
- [ ] 定期验证备份文件的完整性
- [ ] 定期演练恢复流程
- [ ] 备库延迟监控
- [ ] 主从切换演练

## 配置安全

- [ ] 数据库配置文件（my.cnf）不包含明文密码
- [ ] 数据库端口不对外暴露
- [ ] 关闭 LOCAL INFILE（local-infile=0）
- [ ] 关闭符号链接（skip_symbolic_links=yes）
- [ ] 设置 secure_file_priv 限制导入导出目录

## 生成任务表（聚梦画布 · 性能方案）

> [聚梦画布-生成任务性能改造方案](../ai_read/聚梦画布-生成任务性能改造方案.md) §5。MySQL 为 **唯一真相**；Redis/MQ 可重建。

- [ ] `generation_jobs` 保持单表；Poll/抢单 **只查进行中** 状态，管理端列表须时间范围 + 分页
- [ ] `dedupe_key` 唯一索引：API 异步入库后仍作 **最终幂等兜底**
- [ ] 改造新增字段/索引：`next_poll_at`、`(status, next_poll_at)`、`(lane, status, created_at)` 等按方案落地
- [ ] Submit Worker 入库与预扣 **同一事务**；禁止 API 直接写大表绕开算力服务层
- [ ] 协作者任务：`actor_user_id` / billing 用户字段与 `credit_flow` 一致，审计可追溯
