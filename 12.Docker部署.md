# 12. Docker 部署

## 镜像安全

- [ ] 使用官方镜像或可信基础镜像
- [ ] 使用最小化基础镜像（如 python:3.12-slim、alpine）
- [ ] 不在镜像中保留构建工具和临时文件
- [ ] 使用多阶段构建减少镜像体积
- [ ] 定期扫描镜像漏洞（Trivy / Docker Scout）
  ```bash
  trivy image your-image:tag
  ```
- [ ] 删除无用镜像，防止利用已知漏洞镜像
- [ ] 镜像标签不使用 `latest`，使用具体版本号

## 容器运行时

### 用户权限

- [ ] 禁止以 root 运行容器
  ```dockerfile
  RUN groupadd -r appuser && useradd -r -g appuser appuser
  USER appuser
  ```
- [ ] 应用进程使用非 root 用户
- [ ] 容器内不安装 sudo
- [ ] 不使用 privileged 模式

### 资源限制

- [ ] 限制 CPU 使用（--cpus）
- [ ] 限制内存使用（--memory）
- [ ] 限制磁盘使用（--storage-opt）
- [ ] 限制文件描述符数量（--ulimit nofile）
- [ ] 设置 OOM 优先级（--oom-score-adj）

### 文件系统

- [ ] 使用只读文件系统（--read-only）
- [ ] 可写目录挂载临时卷（--tmpfs /tmp）
- [ ] 敏感配置文件使用 Docker Secrets 或挂载卷
- [ ] 不在镜像中硬编码密钥

### 网络

- [ ] 使用自定义网络（docker network create）
- [ ] 只暴露必要端口
- [ ] 不使用 --network host（除非特殊需求）
- [ ] 容器间使用服务名通信

### 其他

- [ ] 设置容器重启策略（--restart unless-stopped）
- [ ] 健康检查（HEALTHCHECK）
- [ ] 设置停止超时（--stop-timeout）
- [ ] 内核能力最小化（--cap-drop ALL --cap-add 必要权限）
- [ ] 禁用新的内核能力（--security-opt no-new-privileges:true）
- [ ] 使用 seccomp 安全配置文件

## Dockerfile 最佳实践

```dockerfile
# 多阶段构建
FROM python:3.12-slim AS builder

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

FROM python:3.12-slim AS runtime

RUN groupadd -r appuser && useradd -r -g appuser appuser

WORKDIR /app
COPY --from=builder /usr/local/lib/python3.12/site-packages /usr/local/lib/python3.12/site-packages
COPY --chown=appuser:appuser . .

USER appuser

EXPOSE 8000
HEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \
    CMD python -c "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"

CMD ["gunicorn", "main:app", "--bind", "0.0.0.0:8000"]
```

## Docker Compose 安全

- [ ] 服务间使用 internal 网络
- [ ] 不暴露内部服务端口到宿主机
- [ ] 使用 .env 文件管理环境变量（不直接写在 docker-compose.yml）
- [ ] 限制服务资源使用
- [ ] 数据库等服务设置密码

## CI/CD 安全

- [ ] 镜像构建前运行安全扫描
- [ ] 镜像推送前检查高危漏洞
- [ ] 使用镜像签名保证完整性
- [ ] 容器运行时限制系统调用
