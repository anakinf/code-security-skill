# 06. Redis 安全

## 访问控制

- [ ] Redis 设置强密码（requirepass，≥32位随机字符串）
- [ ] 关闭公网访问（bind 127.0.0.1 或内网 IP）
- [ ] 不使用默认端口（建议 6379 改为其他端口）
- [ ] 使用 iptables/安全组限制访问来源
- [ ] 禁用危险命令或重命名为空

## 危险命令管理

### 建议禁用/重命名的命令

```
FLUSHALL        → 清空所有数据库
FLUSHDB         → 清空当前数据库
CONFIG          → 修改运行时配置
DEBUG           → 调试命令
KEYS            → 生产环境禁用（使用 SCAN）
SAVE            → 生产环境禁用手动保存
SHUTDOWN        → 关闭服务
SLAVEOF         → 主从配置
CLIENT          → 客户端管理
EVAL/EVALSHA    → Lua 脚本（如不需要）
```

- [ ] 关闭 FLUSHALL（rename-command FLUSHALL ""）
- [ ] 关闭 CONFIG（rename-command CONFIG ""）
- [ ] 关闭 DEBUG（rename-command DEBUG ""）
- [ ] 生产环境禁用 KEYS 命令

## 资源限制

- [ ] 设置最大内存（maxmemory），防止 OOM
- [ ] 设置内存淘汰策略（maxmemory-policy allkeys-lru 或 volatile-lru）
- [ ] 限制最大连接数（maxclients）
- [ ] 设置超时时间（timeout），自动关闭空闲连接
- [ ] 单个 Key 不超过 10MB（大 Key 会导致阻塞）

## Key 管理

- [ ] 所有 Key 设置过期时间（EXPIRE / TTL）
- [ ] Key 命名规范：`项目:模块:业务:ID`（如 `myapp:user:123:tokens`）
- [ ] 定期扫描并清理过期 Key
- [ ] 避免 Big Key（大集合、大字符串）
- [ ] 避免 Hot Key（热点数据分散到多个 Key）

## 缓存一致性

- [ ] 更新数据库后更新缓存（Cache-Aside 模式）
- [ ] 删除数据库记录时删除对应缓存
- [ ] 使用延迟双删策略防止脏数据
- [ ] 缓存设置随机过期时间（基础时间 + 随机偏移），防止缓存雪崩
- [ ] 使用互斥锁（SETNX）防止缓存击穿
- [ ] 缓存穿透防护：布隆过滤器或缓存空值（短过期时间）
- [ ] 不缓存敏感信息（密码、手机号、身份证）
- [ ] 缓存 Key 包含版本号，便于缓存刷新

## 集群安全

- [ ] Redis Cluster 节点间使用密码认证
- [ ] Sentinel 配置认证
- [ ] 集群模式下限制节点间通信端口（16379）
- [ ] 禁用集群节点的外部访问

## 持久化安全

- [ ] RDB 和 AOF 文件存放在安全目录
- [ ] 设置合理的备份策略
- [ ] 数据库文件与 Redis 数据分开放置

## 聚梦画布生产用法（性能方案）

> [聚梦画布-生成任务性能改造方案](../ai_read/聚梦画布-生成任务性能改造方案.md) §6、§10.1、§11.6。生产 Redis TLS，`REDIS_DB=1`，已承载 SMS 验证码。

### Key 与 TTL

| Key 模式 | 用途 | TTL / 注意 |
|----------|------|------------|
| `sms:{scene}:{phone}` | 验证码（现有） | 短 TTL；Redis 不可用 → API **503** |
| `canvas:job:status:{id}` | 任务状态快照 | 普通查询缓存 **5 分钟** |
| `canvas:job:accepted:{id}` | API 已受理 + MQ 已确认 | **30～60 分钟**（非 5 分钟） |
| `canvas:dedupe:{key}` | 幂等占位 → jobId | 按 SLA |
| `canvas:poll:zset:{lane}` | Poll 闹钟 | 丢失可从 MySQL `next_poll_at` 重建 |

- [ ] 任务类 key 统一 **`canvas:` 前缀**，与 SMS key **不冲突**
- [ ] **写穿**：改 `generation_jobs` 后同步更新 Redis；禁止只改库不更缓存（性能方案 §6.1）
- [ ] Redis **不是** 权威余额/扣费结果；job 状态丢失可回源 MySQL
- [ ] Poll 取任务用 **Lua 原子取删**，防多 Poll Worker 重复查同一 job
- [ ] 全局限流令牌桶（上游 QPS）放 Redis，与 SMS 同实例时监控内存与 hot key
