# 07. React 安全

## 构建配置

- [ ] 关闭 React DevTools（生产环境）
  ```js
  // .env.production
  REACT_APP_DISABLE_DEVTOOLS=true
  ```
- [ ] 关闭 SourceMap 生成（production 环境禁用）
  ```js
  // vite.config.js 或 webpack 配置
  productionSourceMap: false
  ```
- [ ] 删除所有 console.log / debugger 语句（使用 babel-plugin-transform-remove-console）
- [ ] 隐藏 .env 文件（.env 文件加入 .gitignore）
- [ ] 隐藏 API 地址（通过代理转发，不暴露真实 API 地址）
- [ ] 关闭开发配置（React.StrictMode 仅用于开发）
- [ ] 开启代码压缩（terser-webpack-plugin / esbuild minify）
- [ ] 开启 Tree Shaking（移除未使用代码）
- [ ] 开启资源 Hash（文件指纹，防止缓存污染）
- [ ] 开启 CSP（Content-Security-Policy）
- [ ] 开启 SRI（Subresource Integrity，外部资源完整性校验）
- [ ] 开启 HSTS（Strict-Transport-Security）

## XSS 防护

- [ ] 使用 JSX 默认转义（React 默认转义，但注意特殊情况）
- [ ] 避免使用 `dangerouslySetInnerHTML`
- [ ] 如必须使用，使用 DOMPurify 净化 HTML
  ```js
  import DOMPurify from 'dompurify';
  <div dangerouslySetInnerHTML={{ __html: DOMPurify.sanitize(html) }} />
  ```
- [ ] 用户输入渲染前进行编码
- [ ] URL 参数渲染前进行编码
- [ ] 不在 `href`、`src` 等属性中直接拼接用户输入
- [ ] 避免使用 `eval`、`new Function`、`setTimeout(string)`

## CSRF 防护

- [ ] 使用 SameSite Cookie（SameSite=Strict 或 Lax）
- [ ] API 增加 CSRF Token
- [ ] 关键操作（修改密码、支付）校验 Referer / Origin

## 敏感信息防护

- [ ] API Token 不存储在 localStorage（使用 HttpOnly Cookie）
- [ ] 如必须使用 localStorage，对敏感数据加密
- [ ] 不在代码中硬编码密钥
- [ ] 不在前端做权限判断唯一依赖（后端必须二次校验）

## 依赖安全

- [ ] 使用 `npm audit` 定期扫描依赖漏洞
- [ ] package.json 锁版本（使用 lockfile）
- [ ] 更新依赖前查看 CVE
- [ ] 删除无用依赖（使用 depcheck 检测）

## 组件安全

- [ ] iframe 设置 sandbox 属性
- [ ] 图片 URL 校验（防止 SSRF 或恶意图片）
- [ ] 文件上传预览限制文件类型
- [ ] 第三方组件保持更新
