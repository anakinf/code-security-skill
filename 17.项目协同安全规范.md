# 17. 项目协同安全规范

> 适用：**聚梦画布**「创建项目 → 邀请成员 → 协同编辑/生成」能力。  
> 目标：**保留现有登录认证设计**，在协同场景下**防越权、防资损、防逻辑漏洞**，且**不阻碍正常协同开发**。

## 1. 与现有认证的关系（必读）

| 层级 | 职责 | 本系统现状 | 规范要求 |
|------|------|------------|----------|
| **认证（Authentication）** | 证明「你是谁」 | HMAC 会话 `userId\|phone\|exp\|sig`，`user_sessions` 校验 | **不改动**现有登录/注册/会话方案 |
| **授权（Authorization）** | 证明「你能对这个项目做什么」 | `project_access.py`：`owner` / `editor` | **必须**在所有带 `projectId` 的接口校验 |
| **计费主体（Billing）** | 证明「算力从谁账上扣」 | 协作者生成扣 **项目 owner** 的 lot | **禁止**协作者扣自己的 lot 冒充项目消费 |

协同安全 **不等于** 重做登录；只是在已有 `get_current_user` 之上增加 **项目级权限** 与 **协作消费规则**。

## 2. 角色与能力边界

```
项目 Owner（projects.owner_id）
  ├─ 邀请 / 移除成员、改协作限额、批准/拒绝生成
  ├─ 删除项目、改协作策略、查看 pending 审批
  └─ 与 Editor 相同：编辑画布、工作流、资产、提交生成

项目 Editor（project_members.status=active, role=editor）
  ├─ 编辑画布、保存工作流、上传/引用资产、提交生成
  ├─ 接受/拒绝/离开邀请、查看自己在项目内的 presence
  └─ 禁止：邀请他人、改 owner 设置、删项目、批准他人 pending 任务

未接受邀请（status=pending）
  └─ 仅可：查看/接受/拒绝邀请；禁止访问项目画布与资源

已移除（status=removed）
  └─ 视为无权限；历史数据审计保留，接口返回 PROJECT_NOT_FOUND
```

**开发约定**：新增协同相关 API 时，先明确属于上表哪一格，再选 `require_project_access` / `require_project_owner` / 邀请专用逻辑。

## 3. 防越权（水平 / 垂直）

### 3.1 水平越权（跨项目、跨用户）

- [ ] 所有读写 **必须** 携带 `projectId`，服务端 **禁止** 仅信任 URL 里的 id 而不查库绑定
- [ ] 资源二次校验：工作流、任务、资产除 `projectId` 外，须校验 `workflow.project_id` / `job.project_id` / 资产索引归属同一项目
- [ ] 当前用户 ID **只** 从会话 Token 解析（`get_current_user`），**禁止**用请求体里的 `userId` 决定权限
- [ ] 非成员访问项目：统一 `PROJECT_NOT_FOUND`（避免泄露项目是否存在）
- [ ] 协作者 **不能** 通过改 `projectId` 访问他人私有项目
- [ ] 协作者 **不能** 读取/修改 owner 的个人算力明细以外的越权数据（如 owner 其他项目）

### 3.2 垂直越权（Editor 做 Owner 的事）

- [ ] 邀请成员、移除成员、更新协作限额：**仅** `require_project_owner`
- [ ] 批准/拒绝协作者生成：**仅** owner
- [ ] 删除项目、转让 owner（若有）：**仅** owner
- [ ] 管理后台接口与用户画布接口分离；后台 **不能** 用普通用户 cookie 调用 admin 路由

### 3.3 邀请链路

- [ ] 仅 owner 可发起邀请；不可邀请自己、不可邀请已是 owner 的账号
- [ ] 邀请前校验成员上限（`MAX_PROJECT_MEMBERS`）
- [ ] 接受邀请：须 **被邀请人本人** 会话 + `status=pending` 行锁（`FOR UPDATE`）
- [ ] 接受时再次校验 active 成员数，防止并发超限
- [ ] 成员变更、接受邀请使用 Redis 互斥锁，防止并发重复写入
- [ ] 移除成员后：立即失去 `require_project_access`；presence 应清理

## 4. 协同算力与资损防护

协同 **允许** 协作者消耗 owner 算力，但必须可审计、可限额、可审批。

| 规则 | 说明 |
|------|------|
| 计费用户 | `resolve_generation_billing_user`：协作者提交 → `billing_user = owner` |
| 行为用户 | `GenerationJob.actor_user_id` 记录实际操作者，便于审计 |
| 预扣归属 | `reserve_for_job` 始终针对 **billing_user** 的 lot |
| 日/总上限 | `collaborator_daily_cap` / `collaborator_total_cap` 仅统计协作者消耗 |
| 审批阈值 | 超过 `collaborator_approval_threshold` → `AWAITING_APPROVAL`，**不预扣**直至 owner 批准 |
| 批准时 | owner 会话 + 再次校验 cap + 对 owner 预扣 + 转 `pending` 入队 |
| 拒绝时 | 置 `failed`，`credit_status=skipped`，不扣费 |

- [ ] 协作者 **不能** 修改 `collaborator_*` 限额字段（仅 owner）
- [ ] 协作者 **不能** 批准自己的 pending 任务
- [ ] 报价仍走 `quote_generation_cost`；**禁止**前端传单价或扣费金额
- [ ] owner 余额不足时，批准 pending 任务须失败且 **不** 部分扣款
- [ ] 生成提交对 billing 用户加锁（`generation_submit_lock`），避免并发超扣

### 异步 Submit 路径（性能方案阶段 2+）

> [聚梦画布-生成任务性能改造方案](../ai_read/聚梦画布-生成任务性能改造方案.md) §3.1、§11.7。

- [ ] API 返回 jobId 前：`quoteToken` 已校验，报价快照在 MQ 消息中；Submit Worker **只** 对 owner（billing_user）预扣
- [ ] 协作者 pending 审批：**批准前不预扣**；批准后入 MQ/submit 路径时仍解析 billing_user = owner
- [ ] 402 变异步：`failed`（余额不足）仍写 Redis，**无** 部分扣款；actor 与 owner 审计字段保留
- [ ] 双轨灰度时协作任务走 **明确 routing**，避免旧 Worker 与 Submit Worker 重复处理

## 5. 数据与存储隔离（协同不破坏项目边界）

- [ ] OSS key 必须含 `projects/{projectId}/`（见 `canvas-data-architecture`）
- [ ] 协作者上传/生成的资产索引写入 **同一项目** `project_assets`
- [ ] 工作流 `revision` 乐观锁：协同编辑冲突返回 409，由前端合并，**禁止** 静默覆盖
- [ ] 节点文本、manifest、batch assets 均带 `projectId` 校验
- [ ] 禁止协作者通过 storage sign-url 接口签名 **其他项目** 的 key

## 6. API 设计检查清单（新增/修改接口时用）

带 `projectId` 的写操作：

1. `get_current_user` 已挂载  
2. 调用 `require_project_access` 或 `require_project_owner`  
3. 若创建 `GenerationJob`，设置 `actor_user_id` 与正确 `billing_user`  
4. 若返回成员列表，不泄露已移除用户的手机号等敏感字段（按现有 schema）  
5. 列表接口分页；邀请 lookup 按手机号查询需限流（防枚举）

## 7. 前端协同开发注意（不阻断功能）

- [ ] UI 根据 `role` / `isCollaborator` 隐藏 owner 专属按钮（**仅 UX**；安全以后端为准）
- [ ] 协作者看到 owner 算力余额时，展示 **项目计费上下文**，避免误以为扣自己钱包
- [ ] pending 审批、邀请面板失败时展示后端 `error_code`，不前端猜测权限
- [ ] **禁止** 在前端 localStorage 缓存「我在某项目的 role」作为授权依据

## 8. 与通用规范的关系

| 文档 | 协同相关章节 |
|------|----------------|
| [02.认证授权规范](./02.认证授权规范.md) | 项目级 RBAC、会话不变 |
| [04.业务安全规范](./04.业务安全规范.md) | 协同算力、审批、成员上限 |
| [03.API安全规范](./03.API安全规范.md) | projectId 校验、防枚举、Idempotency-Key |
| [09.文件上传下载](./09.文件上传下载.md) | 项目目录隔离 |
| [10.AI接口安全](./10.AI接口安全.md) | 协作者生成、审批、actor 审计 |
| [15.上线Checklist](./15.上线Checklist.md) | 协同专项验收 |
| [聚梦画布-生成任务性能改造方案](../ai_read/聚梦画布-生成任务性能改造方案.md) | Submit 异步预扣、MQ、双轨灰度与协作计费 |

## 9. 代码锚点（聚梦画布）

| 模块 | 路径 |
|------|------|
| 项目权限 | `canvas/packages/api/app/services/project_access.py` |
| 成员/邀请 | `canvas/packages/api/app/services/project_members.py` |
| 协作消费/审批 | `canvas/packages/api/app/services/project_collaboration_spend.py` |
| 生成计费主体 | `canvas/packages/api/app/services/generation_submit.py` |
| 项目 API | `canvas/packages/api/app/api/v1/projects.py` |
| 前端邀请 | `canvas/packages/web/src/components/projects/ProjectInvitesPanel.tsx` |

**规范符合性**：上述模块已实现的逻辑，新增功能应 **复用** 这些入口，而不是在路由里手写 `if user_id == owner_id`。
