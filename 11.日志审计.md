# 11. 日志审计

## 操作日志规范

### 记录内容

所有管理员操作必须记录：

- [ ] 操作人（用户 ID、用户名）
- [ ] 操作时间（精确到毫秒）
- [ ] 操作 IP
- [ ] 操作设备（User-Agent）
- [ ] 操作浏览器信息
- [ ] 操作模块（如用户管理、订单管理）
- [ ] 操作类型（CREATE / UPDATE / DELETE / QUERY）
- [ ] 操作描述（如"修改用户角色：user_123 → admin"）
- [ ] 修改前数据（JSON）
- [ ] 修改后数据（JSON）
- [ ] 操作结果（成功/失败）
- [ ] 失败原因

### 记录方式

- [ ] 操作日志使用异步写入，不阻塞主流程
- [ ] 操作日志使用独立数据库表（或专门的日志系统）
- [ ] 操作日志不可修改、不可删除（仅可归档）
- [ ] 操作日志保留 ≥ 180 天（按法规要求）

### 操作回滚

- [ ] 记录修改前数据，支持操作回滚
- [ ] 回滚操作本身也记录日志
- [ ] 回滚操作需二次确认

## 可观测性

### 请求追踪

- [ ] 每个请求分配唯一 RequestId（UUID）
- [ ] RequestId 在请求入口生成，贯穿全链路
- [ ] 响应头中返回 RequestId（`X-Request-Id`）
- [ ] 日志中记录 RequestId，便于问题追踪
- [ ] 支持 TraceId（跨服务追踪）

### 链路追踪

- [ ] 支持 OpenTelemetry / Jaeger / Zipkin
- [ ] 数据库查询耗时（SQL + 慢查询追踪）
- [ ] Redis 操作耗时
- [ ] AI 接口调用耗时
- [ ] 第三方 API 调用耗时
- [ ] 内部函数调用耗时（关键路径）
- [ ] 请求全链路耗时

### 监控指标

- [ ] 接口 QPS（每秒请求数）
- [ ] 接口耗时 P50 / P95 / P99
- [ ] 接口错误率
- [ ] SQL 耗时 P95
- [ ] Redis 耗时 P95
- [ ] AI 接口耗时 P95
- [ ] 第三方接口耗时 P95
- [ ] 慢请求比例（超过 1s 的请求占比）
- [ ] 系统资源（CPU、内存、磁盘、网络）

### 告警

- [ ] 错误率超过阈值时告警（如 > 1%）
- [ ] 慢请求比例超过阈值时告警
- [ ] 接口不可用时告警（5xx 比例）
- [ ] 磁盘使用率超过 80% 时告警
- [ ] AI 接口失败率超过阈值时告警
- [ ] 数据库连接池耗尽时告警
- [ ] Redis 连接失败时告警
- [ ] 告警通知：邮件、短信、钉钉/企微/飞书

## 生成任务可观测性（聚梦画布 · 性能方案）

> [聚梦画布-生成任务性能改造方案](../ai_read/聚梦画布-生成任务性能改造方案.md) §11.9。阶段 0 起采集；安全/资损事件须可告警。

| 指标 | 安全/资损关联 |
|------|----------------|
| MQ 队列深度 / 消费延迟 | submit 堆积 → 长时间「pending」、对账压力 |
| `job:accepted` 有、DB 无 | 异步入库失败，须对账重入队 |
| Submit 异步「余额不足」率 | 402 变异步，防误报攻击或定价 bug |
| `commit_pending` / `release_pending` | 算力未结清，须 reconcile |
| `downloading` 卡住数量 | 收尾失败，防重复扣费/漏结算 |
| DashScope GET QPS / 429 率 | 限流失效可能打爆上游或触发误失败 |
| Redis ZSET 深度 / Poll 延迟 | 轮询调度异常 → 任务悬挂 |
| 单 job 耗时 P90 | 超时退款策略是否误伤 |

- [ ] 日志含 `jobId`、`projectId`、`actor_user_id`（协同时）、`lane`；**不** 记录完整 prompt/媒体 URL 密钥
- [ ] 对账任务（accepted/闹钟重建/credit reconcile）执行记录单独审计

## 审计日志

- [ ] 敏感操作（登录、支付、权限变更）单独审计
- [ ] 审计日志包含：谁、什么时间、做了什么、结果
- [ ] 审计日志不可篡改（append-only）
- [ ] 定期审查审计日志
- [ ] 异常登录检测（异地登录、异常时间登录）

## 日志安全

- [ ] 日志中不记录敏感信息（密码、Token、手机号、身份证）
- [ ] 日志中敏感字段自动脱敏（如 `password: ***`）
- [ ] 日志文件设置合理权限（仅应用用户可读）
- [ ] 日志文件定期归档和清理
- [ ] 日志系统本身需要认证
