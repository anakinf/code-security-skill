# 08. Python 安全

## 高危函数禁用

### eval() / exec()

- [ ] 禁止在生产代码中使用 `eval()`
- [ ] 禁止在生产代码中使用 `exec()`
- [ ] 禁止使用 `compile()` + `exec()` 组合
- [ ] 可在 CI 中使用 Bandit 扫描 `B102`, `B103` 规则

### pickle

- [ ] 禁止对不可信数据使用 `pickle.loads()`
- [ ] 选择安全的序列化方案：`json`、`msgpack`、`protobuf`
- [ ] 如必须使用 pickle，自行实现安全 Unpickler（白名单类）
  ```python
  import pickle

  class SafeUnpickler(pickle.Unpickler):
      ALLOWED = {'dict', 'list', 'str', 'int', 'float', 'bool', 'NoneType', 'tuple'}

      def find_class(self, module, name):
          if name not in self.ALLOWED:
              raise pickle.UnpicklingError(f'Forbidden class: {name}')
          return super().find_class(module, name)
  ```

### yaml

- [ ] 禁止使用 `yaml.load()`（默认使用 `yaml.safe_load()`）
- [ ] `yaml.safe_load()` 不支持自定义类反序列化
- [ ] 使用 `yaml.load()` 时指定 `Loader=yaml.SafeLoader`

### subprocess

- [ ] 禁止 `shell=True`
  - 错误：`subprocess.run(cmd, shell=True)`
  - 正确：`subprocess.run(['command', 'arg1', 'arg2'])`
- [ ] 不使用 `os.system()`、`os.popen()`
- [ ] 使用 `subprocess.run()` 设置 timeout 防止命令挂起
- [ ] 不使用 `os.popen()`、`commands.*`（已废弃）
- [ ] 动态命令参数使用白名单校验

## SSTI（模板注入）

- [ ] 使用 Jinja2 时开启自动转义
  ```python
  env = Environment(autoescape=True)
  ```
- [ ] 用户输入不直接拼接到模板字符串
- [ ] 动态模板名使用白名单校验
- [ ] 不将用户输入作为模板文件路径

## 任意文件读写

- [ ] 路径拼接使用 `os.path.join()` 而非字符串拼接
- [ ] 文件读写前校验路径是否在白名单目录内
- [ ] 路径规范化后校验（`os.path.realpath()` + `startswith()`）
  ```python
  import os

  def safe_path(base_dir, user_path):
      real_path = os.path.realpath(os.path.join(base_dir, user_path))
      if not real_path.startswith(os.path.realpath(base_dir)):
          raise ValueError("Path traversal detected")
      return real_path
  ```
- [ ] 不在文件名中使用用户输入
- [ ] 限制文件读取的目录范围

## 动态导入

- [ ] 限制动态 `__import__()` 和 `importlib.import_module()`
- [ ] 动态模块名使用白名单
- [ ] 不使用 `exec()` 实现动态导入

## 任意对象属性修改

- [ ] 限制 `setattr()`, `delattr()` 的使用
- [ ] 不使用 `object.__dict__` 直接修改属性
- [ ] 关键对象属性定义为 `__slots__`

## 序列化安全

- [ ] 不信任 `__reduce__` 魔术方法
- [ ] 自定义 `__reduce__` 方法不返回危险对象
- [ ] 使用 Marshmallow / Pydantic 进行序列化校验

## CI 自动化检测

| 工具 | 用途 | 命令 |
|------|------|------|
| Bandit | 安全扫描 | `bandit -r src/` |
| Semgrep | 规则引擎 | `semgrep --config=p/python` |
| Safety | 依赖扫描 | `safety check` |
| pip-audit | 依赖审计 | `pip-audit` |
| Flake8 | 代码规范 | `flake8 --select=S src/` |

## FastAPI 特有

- [ ] 关闭文档接口（生产环境）
  ```python
  app = FastAPI(docs_url=None, redoc_url=None)
  ```
- [ ] CORS 配置白名单，不设置 `allow_origins=["*"]`
- [ ] 使用 Pydantic 严格模式（`model_config = {"extra": "forbid"}`）
- [ ] 文件上传使用 `UploadFile` 而非 `bytes`
- [ ] 请求体大小限制
  ```python
  app = FastAPI(max_request_size=10 * 1024 * 1024)
  ```

## 聚梦画布 Worker / 生成链路

> 改造后新增 Submit/Poll runner、MQ 消费、Redis Lua 等路径，须同样遵守本规范（无 `eval`、参数化 SQL、密钥不进日志）。关键目录见 [聚梦画布-生成任务性能改造方案](../ai_read/聚梦画布-生成任务性能改造方案.md) 附录。
