# 03. API 安全规范

## 通用规范

- [ ] 所有 API 使用 HTTPS
- [ ] API 版本化管理（/api/v1/...）
- [ ] 响应统一格式，不暴露内部错误详情
- [ ] 错误信息不包含堆栈、SQL、路径等敏感信息
- [ ] 敏感字段（密码、手机、邮箱）不在响应中返回
- [ ] 列表接口默认分页，限制最大返回条数
- [ ] 批量操作限制单次最大数量
- [ ] 删除接口使用软删除（is_deleted 字段）
- [ ] 关键操作记录操作日志

## 参数校验

- [ ] 所有参数进行类型校验（使用 Pydantic）
- [ ] 字符串参数限制最大长度
- [ ] 数字参数限制取值范围
- [ ] 枚举参数使用 Enum 校验，不接受未定义值
- [ ] 数组参数限制最大元素数量
- [ ] 文件参数限制大小和类型
- [ ] JSON 参数限制最大嵌套深度
- [ ] URL 参数进行编码和校验

## 防刷规范

### 限流

- [ ] AI 生成接口限流
- [ ] 查询接口限流
- [ ] 下载接口限流
- [ ] 上传接口限流
- [ ] 搜索接口限流
- [ ] 导出接口限流
- [ ] 删除接口限流
- [ ] 管理接口限流
- [ ] 每 IP 限流
- [ ] 每用户限流
- [ ] 每 Token 限流
- [ ] 全局限流

### 防重放

- [ ] 幂等性设计（Idempotency-Key）
- [ ] 重复请求检测（Redis + 请求摘要）
- [ ] 时间戳 + nonce 防重放
- [ ] 签名校验（HMAC）

## 接口安全

- [ ] 无关字段自动忽略（使用 Pydantic model）
- [ ] 不使用的接口及时下线
- [ ] 敏感接口增加二次确认
- [ ] 文件下载校验路径穿越
- [ ] 批量操作增加确认机制

## 生成任务提交（聚梦画布 · 性能方案）

> 与 [聚梦画布-生成任务性能改造方案](../ai_read/聚梦画布-生成任务性能改造方案.md) §3.1、§10.2 对齐。改造后 API **不直接 INSERT** `generation_jobs`，安全边界前移。

- [ ] 生成接口 **强制** `Idempotency-Key`（或等价前端必传）；API 用 `dedupe_key` + Redis `SETNX dedupe:{key}->jobId` 返回 **同一 jobId**
- [ ] `quoteToken` / `pricingVersion` 在 **API 层** 校验；已确认报价快照写入 MQ 消息，Submit Worker **不得** 按最新价改扣
- [ ] 鉴权 + `require_project_access` + 模型/限流仍在 API 同步完成
- [ ] 发 MQ 须 **publisher confirm** 成功后再返回 jobId；失败不给新 jobId，客户端用同一 Idempotency-Key 重试
- [ ] 余额：API 可做 Redis 缓存 **预检**；**402 最终以 Submit Worker 事务预扣为准**（异步数秒 `failed`，非安全漏洞但须产品知晓）
- [ ] `GET /generations/{jobId}`：Redis 优先；「已受理未入库」宽限期内返回 `pending`（`job:accepted:{jobId}`），避免误判失败
- [ ] 状态对外映射：内部 `submitted`/`downloading` 等 **不得** 原样返回前端（见性能方案 §6.2）

## 项目隔离（协同 API）

> 聚梦画布所有画布/生成/资产接口携带 `projectId`。详见 [17.项目协同安全规范](./17.项目协同安全规范.md)。

- [ ] 写操作前调用 `require_project_access` 或 `require_project_owner`
- [ ] 读单条资源时校验 `resource.project_id == projectId`
- [ ] 列表接口按 membership 过滤，不返回用户无权项目
- [ ] 手机号 lookup（邀请）单独限流，防用户枚举
- [ ] 成员变更、接受邀请使用分布式锁，防并发重复
